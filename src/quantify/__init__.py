"""Stage 4 — prevalence, co-occurrence, and opportunity scoring.

``python -m src.quantify.run_quantify`` (or ``run_quantification``) reads the
tagged analyzable corpus and writes ``data/processed/opportunity_scores.csv``,
``tag_prevalence.csv``, ``cooccurrence_lift.csv``, and ``segment_matrix.csv``.
"""
